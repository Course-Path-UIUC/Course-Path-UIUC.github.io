
function sortCoursesWithKahns(courses) {
  // courses will be a list of objects, where each object represents a single
  // course selected by the user.

  // It's really important that the returned value is also a list of objects.
  // Please don't modify the objects themselves, just reorder them in the list.
  // Otherwise, the Output component will break.
  return courses;
}

export default sortCoursesWithKahns;
